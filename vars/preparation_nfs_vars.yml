---
  nfs_mount_folder: /mnt
  nfs_share: 10.251.30.1:/nfs
  storage_dest: NFS
  iso_folder: /iso
  ova_folder: /ova
  
  ova_images:
   - dest: "{{ nfs_mount_folder }}"
     ova_name: "alma.ova"
   - dest: "{{ nfs_mount_folder }}"
     ova_name: "alma-temp.ova"   
   - dest: "{{ nfs_mount_folder }}"
     ova_name: "win-temp.ova"       